Place app icons in this directory for electron-builder:

- Windows: `build/icon.ico`
- macOS: `build/icon.icns`
- Linux: `build/icons/` (PNG set, e.g. 256x256)

`build/icon.ico` is the source icon. Keep `build/icon.icns` and `build/icons/*.png` in sync when it changes.
